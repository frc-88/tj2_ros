version: "3"

services:
  build_tj2_ros:
    image: tj2_tools:latest
    container_name: build_tj2_tools
    restart: "no"
    network_mode: "host"
    runtime: "nvidia"
    privileged: true
    group_add:
      - dialout
    volumes:
      - /home/${USER}/docker_ros_ws:/root/ros_ws:rw
      - /home/${USER}/tj2_ros:/root/ros_ws/src/tj2_ros:rw
      - /run/systemd:/run/systemd:rw
      - /dev:/dev:rw
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /home/${USER}/.Xauthority:/home/${USER}/.Xauthority:rw
      # Allow container to control docker
      - /home/${USER}/.docker:/root/.docker:rw # docker looks here inside a container
      - /home/${USER}/.docker:/home/${USER}/.docker:rw # but we need to satisfy spice-lib.sh
      - /var/run/docker.sock:/var/run/docker.sock
    entrypoint: ["/root/scripts/install_tools.sh"]
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              capabilities: [ gpu ]
